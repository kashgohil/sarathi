[project]
name = "sarathi-eval"
version = "0.0.1"
description = "Eval harness for the Sarathi sidecar."
requires-python = ">=3.11,<3.13"

dependencies = [
    "jiwer>=3.0",
    "anthropic>=0.34",
]

[tool.pytest.ini_options]
testpaths = ["harness"]
pythonpath = ["."]
